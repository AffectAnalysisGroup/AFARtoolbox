function avgIOD = getAvgIOD(fit)

  total_frame = size(fit,2);
  IOD = zeros(1,total_frame);
  for frame_index = 1 : size(fit,2)  
      left_corner_dist = 
      current_IOD = 
      IOD(frame) 
      continue;
  end

end
